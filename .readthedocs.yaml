version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "3"
  apt_packages:
    - libdjvulibre-dev
    - graphviz

sphinx:
  configuration: doc/api/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
